// svc-billing-webhooks — nhận webhook Stripe/PayOS, chuẩn hóa về Transaction.
// Triển khai đầy đủ ở GIAI ĐOẠN 5 theo CLAUDE.md (đường tiền — bắt buộc có test idempotency).
console.log(
  "svc-billing-webhooks: khung Giai đoạn 0 — webhook Stripe/PayOS dựng ở Giai đoạn 5.",
);
