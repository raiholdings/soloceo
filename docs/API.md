# SoloCEO API

File này được sinh tự động từ OpenAPI của api-core (Swagger tại `/docs`).
Đặc tả nguồn: CLAUDE.md Phần 6.

Giai đoạn 0: mới có `GET /v1/health`. Các module orgs/ventures/store/payments/
marketplace/admin được bổ sung từ Giai đoạn 1 trở đi.
