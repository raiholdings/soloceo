# ⚠️ DEPRECATED (v2 cleanup — R0 §C8)

Cụm **OpenClaw / Claw3D / ClawHub** đã được TÁCH sang dự án độc lập **openclawos.vn**
(repo riêng). Thư mục này KHÔNG còn thuộc luồng chạy của SoloCEO OS v2.

- KHÔNG xoá (giữ tham chiếu lịch sử).
- Không nằm trong pnpm workspace / turbo build.
- SoloCEO v2 thay thế bằng: DeerFlow (lõi) + AIO Sandbox + FlowGram + skills/vietnam-business.

Xem: `research/R0-hien-trang-va-loai-bo.md`, `SOLOCEO-DEERFLOW-SPEC.md` §4-C.
