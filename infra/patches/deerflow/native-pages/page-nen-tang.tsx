import { SoloceoNenTang } from "@/components/workspace/soloceo-nen-tang";

// Nền tảng — cổng dịch vụ PaaS + tên miền (WHMCS platform.soloceo.vn) nhúng trong workspace.
export default function NenTangPage() {
  return <SoloceoNenTang />;
}
